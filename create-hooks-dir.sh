#!/usr/bin/bash

cd src/core/hooks
# Check if something is directory

# files have camelCase use "-" but for now, let us do it manually



mkdir use-app-routes use-session-storage use-local-storage use-throttle use-debounce \
      use-abort-controller use-auth use-async use-previous use-os use-network use-indexed-db \
      use-isomorphi-effect use-idle use-require-auth use-router use-safe-state use-safe-context \
      use-safe-cache use-resize-observer use-scroll-lock use-scroll-into-view use-list-state \
      use-hot-keys use-fullscreen use-focus-within use-timeout use-window-event use-window-scroll \
      use-click-outside use-document-title
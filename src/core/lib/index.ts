/**
 * 
 * *Stores facades of external libraries ::
 * ? 1. Custom React Router
 * ? 2. Custom React Query
 * ? 3. Custom React Hook Form
 * ? *. --more
 * TODO: Build a custom React Router that adds a middleware before next route loads
 * 
*/

export { }
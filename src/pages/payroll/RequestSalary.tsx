import React from 'react'
import { useDocumentTitle } from '../../core/hooks/use-document-title'

// Trigger a message on the admin InApp inbox feature/ notification

export function RequestSalary() {
    useDocumentTitle('Request Salary')
    return (
        <>
            {/* Request payment button :: Display pending salary */}
        </>
    )
}